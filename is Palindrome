class Solution:
    def isPalindrome(self, s: str) -> bool:
        res=''
        for i in s:
            if i.isalpha() or i.isdigit():
                res+=i
        res=res.lower()
        if res==res[::-1]:
            return True
        else:
            return False
        
